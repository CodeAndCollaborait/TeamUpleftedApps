package com.spring.tutorial.SpringTutorials.annotations;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.context.annotation.AnnotationConfigApplicationContext;
import org.springframework.stereotype.Component;

public class AnnotationApp {

/*
    @Autowired
    private static AboutAutoWired autoWired;
    public static void methodCalls(){

        AnnotationConfigApplicationContext context = new AnnotationConfigApplicationContext(AboutBean.class);

        CustomerService service = context.getBean(CustomerService.class);
        service.getCustomerService();
        System.out.println(service.hashCode());

        CustomerService serviceTwo = (context.getBean(CustomerService.class));
        serviceTwo.getCustomerService();
        System.out.println(serviceTwo.hashCode());

        //Hash codes are the same because service and serviceTwo are in the same container


        //calling bean by name instead of class
        OrderService orderService = (OrderService) context.getBean("order");
        orderService.placeOrderService();
        System.out.println(orderService.hashCode());
        autoWired.setClassName("Autowired Class");
        autoWired.setObjname("Autowire object");
        System.out.println(autoWired.getClassName());
        System.out.println(autoWired.getObjname());

        AnnotationApp.methodCalls();

        User user = context.getBean(User.class);
       // user.setId(1005);
        System.out.println(user);
    }
    // 1. @configuration - creates container (class that uses bean)
// 2. Create bean
//Spring will take care of object life cycle and implement a design pattern

 */
    }


